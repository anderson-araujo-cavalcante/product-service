using System.Linq.Expressions;

namespace WT.Product.Domain.Services.Interfaces
{
    //public interface IServiceBase<TEntity> : IServiceBase where TEntity : class
    //{
    //    Task<IEnumerable<TEntity>> GetAllAsync();
    //    Task<IEnumerable<TEntity>> GetAllAsync(Expression<Func<TEntity, bool>> predicate, Func<IQueryable<TEntity>, IQueryable<TEntity>> include);

    //    TEntity Find(int id, bool includeRemoved = false);
    //    TEntity Find(Expression<Func<TEntity, bool>> predicate, bool includeRemoved = false);
    //    TEntity Find(Expression<Func<TEntity, bool>> predicate, Func<IQueryable<TEntity>, IQueryable<TEntity>> include);


    //    void Add(TEntity entity);
    //    void AddRange(IEnumerable<TEntity> entities);
    //    void AddOrUpdate(TEntity entity);

    //    void Update(TEntity entity);
    //    void Update(Action<TEntity> where, Action<TEntity> action);
    //    void Update(TEntity entity, Action<TEntity> action);
    //    void Update(TEntity entity, TEntity updatedEntity);

    //    void Remove(TEntity entity);
    //    void Remove(TEntity entity, bool isHardRemoval = false);
    //    void RemoveWhere(Expression<Func<TEntity, bool>> predicate);
    //    void RemoveRange(IEnumerable<TEntity> entities);
    //}

    //public interface IServiceBase : IDisposable
    //{
    //    //Task<IEnumerable<T>> QueryAsync<T>(string sql, object param = null);
    //}
}
